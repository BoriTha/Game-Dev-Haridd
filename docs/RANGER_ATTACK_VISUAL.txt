╔══════════════════════════════════════════════════════════════════════════════╗
║                  RANGER 3-STATE BOW ATTACK ANIMATION SYSTEM                  ║
║                           (Reusable Pattern v0.7)                            ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│                            STATE FLOW DIAGRAM                                │
└──────────────────────────────────────────────────────────────────────────────┘

    Player Input Timeline:
    ──────────────────────────────────────────────────────────────────────────
    [LMB Down]    [Holding LMB...]                [LMB Release]
         │              │                                │
         ▼              ▼                                ▼
    ┌─────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
    │  IDLE   │──▶│  CHARGE  │──▶│ CHARGED  │──▶│  SHOOT   │──▶│  IDLE    │
    └─────────┘   └──────────┘   └──────────┘   └──────────┘   └──────────┘
     Default      Drawing bow    Hold at full    Release bow    Return to
     state        (progressive)   draw (loop)    (fire arrow)   default

    Animation Details:
    ├─ CHARGE:   4 frames × 5 duration = 20 frames (~0.33s at 60 FPS)
    ├─ CHARGED:  1 frame × 1 duration = loops until release
    └─ SHOOT:    2 frames × 4 duration = 8 frames (~0.13s at 60 FPS)


┌──────────────────────────────────────────────────────────────────────────────┐
│                          ANIMATION FRAME BREAKDOWN                           │
└──────────────────────────────────────────────────────────────────────────────┘

CHARGE Animation (Drawing Bow):
┌──────┬──────┬──────┬──────┐
│ na-1 │ na-2 │ na-3 │ na-4 │  ──▶  Auto-transition to CHARGED
└──────┴──────┴──────┴──────┘
  ↓      ↓      ↓      ↓
 Start   25%    50%    75%    (Progressive draw visualization)
  ●──    ●───   ●────  ●─────

Frame Duration: 5 frames each
Total Duration: 20 frames
Loop: NO (transitions when complete)
Priority: 4 (overrides movement)


CHARGED Animation (Full Draw Hold):
┌──────┐
│ na-5 │ ─▶ ─▶ ─▶ ─▶  (Loops indefinitely)
└──────┘
  ↓
 100%   Fully drawn, ready to fire
 ●──────▶

Frame Duration: 1 frame
Total Duration: Infinite (loops until release)
Loop: YES
Priority: 4


SHOOT Animation (Arrow Release):
┌──────┬──────┐
│ na-5 │ na-6 │  ──▶  Auto-transition to IDLE
└──────┴──────┘
  ↓      ↓
Release  Follow
 start   through
  ╰─►     ╰─►  (Arrow spawned during this animation)

Frame Duration: 4 frames each
Total Duration: 8 frames
Loop: NO (transitions to IDLE)
Priority: 4 (cannot be interrupted)


┌──────────────────────────────────────────────────────────────────────────────┐
│                          CODE IMPLEMENTATION MAP                             │
└──────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────┐
│ INPUT HANDLING      │  (player_entity.py lines 791-829)
├─────────────────────┤
│ if lmb and not      │ ──▶ Start charging
│   self._prev_lmb:   │     self.charging = True
│   ...               │     self.charge_time = 0
│                     │
│ if self.charging    │ ──▶ Increment charge timer
│   and lmb:          │     self.charge_time += 1
│   ...               │
│                     │
│ if self.charging    │ ──▶ Fire arrow on release
│   and not lmb:      │     self.fire_arrow(...)
│   ...               │     self.charging = False
└─────────────────────┘

         │
         ▼

┌─────────────────────┐
│ ANIMATION STATE     │  (player_entity.py lines 483-555)
│ MACHINE             │
├─────────────────────┤
│ if self.charging:   │
│   if charge_time >= │ ──▶ Play CHARGED (full draw hold)
│     threshold:      │
│     ...CHARGED      │
│   else:             │ ──▶ Play CHARGE (drawing bow)
│     ...CHARGE       │
└─────────────────────┘

         │
         ▼

┌─────────────────────┐
│ FIRE ARROW          │  (player_entity.py lines 984-1008)
├─────────────────────┤
│ def fire_arrow():   │
│   anim_manager.play │ ──▶ Trigger SHOOT animation
│   (SHOOT)           │
│                     │
│   spawn_arrow()     │ ──▶ Create arrow projectile
└─────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                         STATE VARIABLE TRACKING                              │
└──────────────────────────────────────────────────────────────────────────────┘

Timeline of variable states during a full attack cycle:

Frame:    0     10     20     30     40     50     60     70
          │      │      │      │      │      │      │      │
LMB:      ▼──────────────────────────▲      │      │      │
          Down    Holding...       Release   │      │      │
                                             │      │      │
charging: F ─────▶ T ──────────────▶ T ─────▶ F ───▶ F ───▶ F
                   │                 │        │
charge_   0 ─────▶ 1 ──▶ 10 ──▶ 30 ──▶ 30 ──▶ 0 ───▶ 0 ───▶ 0
time:              │       │       │
                   │       │       │
Animation: IDLE ──▶│CHARGE │ CHARGED│ SHOOT ▶ IDLE ▶ IDLE
                   └───────┤threshold│
                          (30 frames)

Legend:
  F = False, T = True
  ▼ = Mouse button press
  ▲ = Mouse button release
  threshold = self.charge_threshold (30 frames = 0.5 seconds at 60 FPS)


┌──────────────────────────────────────────────────────────────────────────────┐
│                    ANIMATION PRIORITY HIERARCHY                              │
└──────────────────────────────────────────────────────────────────────────────┘

    Priority     Animation State         Interruptible?
    ────────     ───────────────         ──────────────
       6         SKILL_1                 No
       5         ATTACK                  No  
       4         DASH                    Yes (by higher priority)
       4         SHOOT                   No (protected by is_playing check)
       4         CHARGE/CHARGED          Yes (by DASH or higher)
       3         WALL_SLIDE              Yes
       2         JUMP/FALL               Yes
       1         RUN                     Yes
       0         IDLE                    Always (lowest priority)

    Higher priority animations OVERRIDE lower priority ones.
    Same priority animations checked in code order.


┌──────────────────────────────────────────────────────────────────────────────┐
│                         QUICK ADAPTATION CHECKLIST                           │
└──────────────────────────────────────────────────────────────────────────────┘

To use this system for a new character:

  ☐ 1. Add state variables:
       - self.charging (bool)
       - self.charge_time (int)
       - self.charge_threshold (int)
       - self._prev_[input]_key (bool)

  ☐ 2. Load 3 animations:
       - CHARGE (loop=False, next_state=CHARGED, priority=4)
       - CHARGED (loop=True, priority=4)
       - SHOOT (loop=False, next_state=IDLE, priority=4)

  ☐ 3. Implement input handling:
       - Press attack → charging = True, charge_time = 0
       - Hold attack → charge_time += 1
       - Release attack → fire_attack(), charging = False

  ☐ 4. Add animation state machine logic:
       - Priority 1: SHOOT (protected, must complete)
       - Priority 2: CHARGE/CHARGED (based on charge_time)
       - Priority 3+: Movement animations

  ☐ 5. Trigger SHOOT in attack method:
       - self.anim_manager.play(AnimationState.SHOOT)

  ✓ Done! The system handles all transitions automatically.


┌──────────────────────────────────────────────────────────────────────────────┐
│                              ASSET STRUCTURE                                 │
└──────────────────────────────────────────────────────────────────────────────┘

assets/Player/Ranger/attk-adjust/
├── charge/
│   ├── na-1.png    ◀── Start drawing bow
│   ├── na-2.png    ◀── Quarter drawn
│   ├── na-3.png    ◀── Half drawn
│   └── na-4.png    ◀── Almost fully drawn
├── charged/
│   └── na-5.png    ◀── Fully drawn (loops)
└── shoot/
    ├── na-5.png    ◀── Release start (same as charged pose)
    └── na-6.png    ◀── Follow-through


┌──────────────────────────────────────────────────────────────────────────────┐
│                         COMMON USE CASES                                     │
└──────────────────────────────────────────────────────────────────────────────┘

1. QUICK SHOT (tap attack button):
   ─────────────────────────────────
   LMB Down → LMB Up (within 10 frames)
   
   Timeline: IDLE → CHARGE (partial) → SHOOT → IDLE
   Result: Weak arrow (3 damage, 10 speed)
   Duration: ~18 frames (~0.3 seconds)

2. CHARGED SHOT (hold for 0.5+ seconds):
   ────────────────────────────────────────
   LMB Down → Hold 30+ frames → LMB Up
   
   Timeline: IDLE → CHARGE → CHARGED (loop) → SHOOT → IDLE
   Result: Strong arrow (7 damage, 14 speed, pierce)
   Duration: ~58 frames (~1.0 second)

3. CANCEL CHARGE (dash while charging):
   ───────────────────────────────────────
   LMB Down → DASH key → LMB Release
   
   Timeline: IDLE → CHARGE → DASH → SHOOT → IDLE
   Result: Arrow fires mid-dash, DASH overrides CHARGE
   Note: SHOOT still plays after dash completes


┌──────────────────────────────────────────────────────────────────────────────┐
│                            TROUBLESHOOTING                                   │
└──────────────────────────────────────────────────────────────────────────────┘

Problem:  Animation stuck in CHARGE, doesn't transition to CHARGED
Solution: Check that CHARGE has next_state=AnimationState.CHARGED

Problem:  SHOOT animation gets cut off / character returns to IDLE too fast
Solution: Ensure SHOOT priority check: if current == SHOOT and is_playing: return

Problem:  Character faces wrong direction while charging
Solution: Add mouse-facing lock (lines 731-742 in player_entity.py)

Problem:  Can't move while charging
Solution: Remove any movement blocking in input() - movement should work independently

Problem:  Charge time doesn't increment
Solution: Check that charge_time += 1 is inside "if self.charging and lmb" block


╔══════════════════════════════════════════════════════════════════════════════╗
║                      DOCUMENTATION & EXAMPLES                                ║
╚══════════════════════════════════════════════════════════════════════════════╝

  📄 Full Documentation:     docs/RANGER_ATTACK_SYSTEM.md
  💡 Working Example:        docs/RANGER_ATTACK_EXAMPLE.py
  🚀 Quick Start:            docs/RANGER_ATTACK_QUICK_START.md
  📊 This Diagram:           docs/RANGER_ATTACK_VISUAL.txt

  Implementation Location:   src/entities/player_entity.py
    - Lines 312-420:         _load_ranger_animations()
    - Lines 483-555:         _update_ranger_animations()
    - Lines 791-829:         input() attack handling
    - Lines 984-1008:        fire_arrow()

╔══════════════════════════════════════════════════════════════════════════════╗
║                            STATUS: ✅ PRODUCTION READY                       ║
║                            Version: 0.7 | January 2025                       ║
╚══════════════════════════════════════════════════════════════════════════════╝
